# README Gap Analysis

Generated by docsync on 2026-05-21.

## Classification

`README.md` is partially stale.

The product direction, Phase 1/Phase 2 framing, stack choice, security model, and local development commands are mostly aligned with the codebase. The stale parts are concentrated around implementation status, repository structure, backend/frontend runtime details, test coverage, and env variables added during the Phase 1 realtime-token/WebRTC work.

## Checklist

| Area | Status | Notes |
| --- | --- | --- |
| Project description | Accurate | SimTalk remains a private realtime speech translation prototype using OpenAI Realtime Translate. |
| Architecture | Partially stale | The browser/backend/OpenAI split is accurate, but README under-described the implemented `POST /realtime/token` and WebRTC startup path. |
| Tech stack | Mostly accurate | React/Vite/TypeScript/Hono/Zod/pnpm are accurate. Tailwind/shadcn are planned only; they are not installed. |
| Folder structure diagram | Stale | README listed planned folders and missing `.github/workflows`; actual repo has implemented routes/services/middleware and no CI/deploy config. |
| Setup and run instructions | Mostly accurate | Root `pnpm dev`, `pnpm typecheck`, `pnpm test`, `pnpm build`, and `pnpm test:e2e` exist. README should mention shared-types build prerequisite is handled by scripts. |
| Environment/config guidance | Incomplete | README omitted `OPENAI_REALTIME_CLIENT_SECRET_URL`, `OPENAI_REALTIME_CLIENT_SECRET_TTL_SECONDS`, `REALTIME_TOKEN_RATE_LIMIT_WINDOW_MS`, and `REALTIME_TOKEN_RATE_LIMIT_MAX_REQUESTS`. |
| Testing | Partially stale | README described desired test locations, but it should reflect actual root `tests/` layout and current coverage. |
| Deployment | Mostly planned | Vercel/Cloud Run direction is in docs, but no in-repo deployment config or CI exists. README should state that plainly. |
| Usage examples | Incomplete | README should include local API examples for `/health` and `/realtime/token` plus note that real WebRTC needs browser permissions and OpenAI config. |
| Feature list | Partially stale | README should distinguish implemented Phase 1 foundation from pending mode-specific flows and local recording. |
| Recent changes | Missing | README should note implemented token endpoint and WebRTC scaffold. |
| Known limitations | Incomplete | README should mention no CI/deploy config, no local recording yet, limited E2E scope, in-memory rate limiting, and out-of-tree Vercel settings. |

## Required README Updates

- Update Project Status to describe current implemented slices:
  - Shared Zod contracts.
  - Hono backend with `/health` and `/realtime/token`.
  - OpenAI realtime client-secret service.
  - React frontend token flow and browser-native WebRTC startup.
  - Transcript delta preview.
  - Unit/component/integration/E2E tests.
- Update Technology Stack:
  - Keep CSS variables as current UI styling.
  - Keep Tailwind/shadcn as planned only, not installed.
- Replace repository tree with the actual current structure.
- Update Architecture Overview with the concrete `POST /realtime/token` and browser-to-OpenAI WebRTC flow.
- Update Environment Variables with all variables in `.env.example` files.
- Update Testing Strategy with exact test paths and current E2E scope.
- Update Local Development and Usage Examples with:
  - `GET /health`
  - `POST /realtime/token`
  - note that OpenAI token minting needs `OPENAI_API_KEY`.
- Clarify deployment status:
  - Vercel is the Phase 1 target.
  - Project settings/password protection/domain are out-of-repo.
  - No `.github/workflows`, Dockerfile, or Vercel config exists yet.
- Add current limitations:
  - Mode-specific flows are not fully implemented.
  - Browser-local recording/download is not implemented.
  - Playwright is a frontend smoke test only.
  - In-memory rate limiting is best-effort and may reset with process/serverless lifecycle.
  - Agentic subsystem notes are stale and should be refreshed separately if desired.

## Things Not To Claim

- Do not claim Tailwind CSS or shadcn/ui are installed.
- Do not claim local recording is implemented.
- Do not claim CI/CD exists.
- Do not claim backend auth or a single-user allowlist is implemented in code; Phase 1 access control is planned through Vercel Password Protection/out-of-tree settings.
- Do not claim the backend proxies audio or transcripts.
- Do not claim server-side persistence exists.
