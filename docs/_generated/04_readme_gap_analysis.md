# README Gap Analysis

Generated by docsync on 2026-05-23.

## Classification

`README.md` is partially stale.

The high-level product, Phase 1/Phase 2 framing, core architecture, security/privacy model, local development commands, and deployment caveats are mostly accurate. The stale areas are implementation status, frontend structure, E2E scope, env variable coverage, and wording around OpenAI `gpt-realtime-translate` capabilities.

## Checklist

| Area | Status | Notes |
| --- | --- | --- |
| Project description | Incomplete | Correctly names realtime translation and `gpt-realtime-translate`; should explain dynamic voice adaptation carefully. |
| Architecture | Mostly accurate | Browser/backend/OpenAI split matches code. Add that OpenAI translated audio uses adaptive voice rendering. |
| Tech stack | Mostly accurate | React/Vite/Hono/Zod/pnpm are accurate; active CSS path and component structure need refresh. |
| Folder structure diagram | Stale | Omits `frontend/src/components/*`, `frontend/src/styles/tokens.css`; lists removed `styles.css`. |
| Setup and run instructions | Mostly accurate | Commands exist; frontend actually binds to `127.0.0.1`, while `localhost` is also accepted for CORS. |
| Environment/config guidance | Incomplete | Omits `OPENAI_REALTIME_INPUT_TRANSCRIPTION_MODEL`; should note `APP_URL` is placeholder/not read by code. |
| Testing | Partially stale | E2E coverage is more than shell smoke: it mocks token/OpenAI paths and covers launch/session/summary flows. |
| Deployment | Current | Correctly states no in-repo CI/deploy config. |
| Usage examples | Mostly accurate | API examples align with shared schema. |
| Feature list | Partially stale | Mode UIs, recording, transcript download, and audio download are partially implemented. |
| Recent changes | Missing | Recent commits added language coverage and Talk/Turn-about recording/transcription fixes. |
| Known limitations | Partially stale | Should shift from "not implemented" to "implemented but not fully validated/polished". |

## Required README Updates

- Add a `gpt-realtime-translate` description explaining streaming speech-to-speech translation and dynamic voice adaptation for general tone, pitch, and style.
- Avoid promising exact voice cloning or guaranteed speaker gender matching; public docs do not guarantee exact identity/gender matching.
- Refresh Project Status:
  - Mode surfaces and browser-local recording/download are now partially implemented.
  - Manual live OpenAI/browser validation and production CI/deploy remain pending.
- Refresh Technology Stack:
  - Active styling is CSS variables in `frontend/src/styles/tokens.css`, brand primitives, and component styles.
  - Tailwind/shadcn are not installed; shadcn scaffold files are not active.
- Replace Project Structure with the actual current tree.
- Update env docs:
  - Add `OPENAI_REALTIME_INPUT_TRANSCRIPTION_MODEL`.
  - Clarify `APP_URL`, `SESSION_SECRET`, and `VERCEL_PROTECTION_BYPASS_SECRET` are placeholders/not consumed by code.
- Update Testing and Roadmap/Known Limitations:
  - E2E tests cover mocked launch/session paths.
  - Realtime WebRTC still needs manual live validation.

## Things Not To Claim

- Do not claim Tailwind CSS or shadcn/ui are installed.
- Do not claim CI/CD exists.
- Do not claim backend auth or a single-user allowlist is implemented in code; Phase 1 access control is through out-of-repo Vercel settings.
- Do not claim the backend proxies audio or transcripts.
- Do not claim server-side persistence exists.
- Do not claim exact source-speaker voice cloning or guaranteed gender matching by OpenAI.
