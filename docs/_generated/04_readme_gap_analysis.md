# README Gap Analysis

Generated by docsync on 2026-05-24.

## Classification

`README.md` is partially stale.

The high-level product description, security/privacy invariants, local development basics, environment tables, and API examples are mostly aligned with code. The main drift is freshness: committed CI/deploy now exists, the Vercel adapter is `api/index.ts`, Phase 1.5 LiveKit remote rooms are implemented and tested, and the folder/test sections do not fully reflect those additions.

## Checklist

| Area                        | Status          | Notes                                                                                                                                                      |
| --------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Project description         | Partially stale | Correctly describes realtime translation and local modes, but the intro still frames remote rooms as Phase 2 even though two-person Phase 1.5 rooms exist. |
| Architecture                | Mostly accurate | Browser/backend/OpenAI split is right and backend section mentions room routes, but README needs a remote-room runtime flow.                               |
| Tech stack                  | Incomplete      | Missing active LiveKit client/server SDK usage and CI/CD status.                                                                                           |
| Folder structure diagram    | Stale           | Missing `api/`, `vercel.json`, `.github/workflows/`, room routes/services, frontend room modules, and Vercel adapter test.                                 |
| Setup and run instructions  | Mostly accurate | Core commands exist; root lint/format scripts and `typecheck:api` are not documented.                                                                      |
| Environment/config guidance | Mostly accurate | Env vars match code; frontend env example still has a commented legacy two-project Vercel URL.                                                             |
| Testing                     | Partially stale | Does not mention room route/service tests, room frontend clients, LiveKit session tests, Vercel adapter test, or remote-room E2E UI coverage.              |
| Deployment                  | Partially stale | Deployment section mostly accurate, but health check wording implies `/health` works on Vercel and adapter path is wrong.                                  |
| Usage examples              | Accurate        | Local API examples align with shared schemas for realtime and rooms.                                                                                       |
| Feature list                | Partially stale | Omits Phase 1.5 LiveKit rooms from "Implemented now".                                                                                                      |
| Recent changes              | Incomplete      | Recent Vercel API adapter/deploy hardening and LiveKit room fixes need reflection.                                                                         |
| Known limitations           | Stale           | Says no CI/CD pipeline is committed, which is now false.                                                                                                   |

## Consistency And Completeness Audit

Undocumented or under-documented runtime paths:

- Vercel API adapter in `api/index.ts`, mounted under `/api`.
- Remote room flow through `POST /rooms`, `POST /rooms/:roomId/token`, LiveKit connect, and OpenAI translation of the subscribed remote microphone track.
- CI pipeline and Vercel deploy workflow under `.github/workflows/`.
- `pnpm lint`, `pnpm format`, `pnpm format:check`, and `pnpm typecheck:api`.

Dead, stale, or misleading files/folders:

- `components.json`, `frontend/src/components/ui/*`, and `frontend/src/lib/utils.ts` are unused shadcn-style scaffold remnants.
- `docs/ux-redesign-plan.md` still reads like a proposal after much of the UX redesign landed.
- `.agentic/SUBSYSTEMS/*`, `.agentic/PROJECT_BRIEF.md`, and `.agentic/LESSONS/decisions.md` contain planned-status, old domain, or stale Tailwind/shadcn claims.
- `PRD.md` still frames remote rooms as Phase 2.

Architecture described in docs but not present in code:

- In-repo backend auth / single-user allowlist is not present; Vercel Password Protection is out-of-repo.
- Database, persistence, object storage, queues, workers, Docker, and Cloud Run configs are not present.
- Tailwind/shadcn runtime usage is not present.

Code present in repo but absent or weak in README:

- LiveKit room backend service and route.
- LiveKit frontend room session bridge and room token client.
- Vercel adapter and API typecheck target.
- CI/deploy workflows.
- Expanded room/LiveKit test coverage.

## Required README Updates

- Reconcile status wording: Phase 1 is local modes; Phase 1.5 has two-person LiveKit remote rooms; Phase 2 is accounts, persistence, and larger rooms.
- Add LiveKit to the current stack and move "LiveKit or equivalent" out of the Phase 2-only framing.
- Add remote-room flow to the architecture section.
- Refresh "Implemented now" with room routes/services, frontend room modules, LiveKit SDKs, and tests.
- Refresh the project structure tree with `api/`, `.github/workflows/`, `vercel.json`, room files, and frontend room modules.
- Document `pnpm lint`, `pnpm format:check`, `pnpm format`, and `typecheck:api` as part of the local/CI workflow.
- Correct Vercel health check wording to `GET /api/health` for deployed environments and local `GET /health` for local backend.
- Correct `api/[...route].ts` to `api/index.ts`.
- Update Testing for room and Vercel adapter coverage and clarify E2E remains mocked.
- Remove the Known Limitations claim that no CI/CD pipeline is committed and mark deployment/CI configuration as done in the roadmap.

## Things Not To Claim

- Do not claim Tailwind CSS or shadcn/ui are installed.
- Do not claim backend auth or a single-user allowlist is implemented in code; Phase 1 access control is through out-of-repo Vercel settings.
- Do not claim the backend proxies audio or transcripts.
- Do not claim server-side persistence exists.
- Do not claim live OpenAI or live LiveKit behavior is fully validated by automated tests.
- Do not claim exact source-speaker voice cloning or guaranteed gender matching by OpenAI.
