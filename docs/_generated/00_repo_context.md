# Repository Context

Generated by docsync on 2026-05-23.

## Git Context

- Recent commits focus on frontend language coverage, Talk/Turn-about fixes, recording fixes, and UI content updates:
  - `09a456d` added language filter to selector.
  - `10ebd1d` expanded input/output language support.
  - `e0a3087` fixed Talk mode transcription and recording input.
  - `3eaceb6` fixed Turn-about token re-minting.
  - `a1469bd` added transcription to Talk mode Turn-about.
- A read-only `git status --short` and `git diff --stat` during docsync returned no output. The initial Cursor snapshot for this session showed modified frontend/test files, so treat working-tree state as time-sensitive.

## Project State

- Status: pre-MVP private Phase 1 prototype.
- Current implemented slice:
  - pnpm workspace with `frontend`, `backend`, and `shared/types`.
  - Shared Zod contracts for modes, language tags, token request/response, API errors, and health responses.
  - Hono backend with `GET /health`, `POST /realtime/token`, strict CORS, security headers, in-memory token rate limiting, and OpenAI client-secret minting.
  - React/Vite frontend with UX-redesigned Lobby, Listener, Turn-about, Practice, transcript, dev drawer, and Summary screens.
  - Browser-native WebRTC setup, transcript deltas, remote audio playback, cleanup, listener recording, practice recording, transcript copy/download, and audio download when a recording exists.
  - Vitest and Playwright coverage for shared contracts, backend routes/services/config, frontend token/WebRTC modules, component flows, and mocked E2E flows.
- Still pending:
  - Manual realtime validation with a real OpenAI key, microphone permissions, and supported browsers.
  - Production CI/deploy configuration.
  - Polishing mode behavior and recording coverage beyond the current local browser implementation.

## Source Files Reviewed

- Product/docs: `README.md`, `PRD.md`, `System_Architecture.md`, `docs/ux-redesign-plan.md`, prior `docs/_generated/*.md`.
- Agentic memory: `.agentic/PROJECT_BRIEF.md`, `.agentic/MEMORY_INDEX.md`.
- Root config: `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `playwright.config.ts`, `.gitignore`, `components.json`.
- Backend: `backend/package.json`, `backend/.env.example`, `backend/src/config.ts`, `backend/src/services/openAiRealtime.ts`, and backend files reviewed by docsync subagents.
- Frontend: `frontend/package.json`, `frontend/.env.example`, `frontend/src/App.tsx`, `frontend/src/components/screens/Summary.tsx`, `frontend/src/components/brand/languages.ts`, `frontend/src/styles/tokens.css`, and frontend files reviewed by docsync subagents.
- Shared: `shared/types/package.json`, `shared/types/src/index.ts`.
- Tests: `tests/e2e/home.spec.ts` and repo test files reviewed by docsync subagents.

## Config And Environment

- Package manager: pnpm 10.16.1 via root `packageManager`.
- Node engine: Node.js >= 22.
- Workspace packages:
  - `@simtalk/frontend`
  - `@simtalk/backend`
  - `@simtalk/shared-types`
- No in-repo `.github/workflows`, Dockerfile, Vercel config, database config, or migration system was found.
- `WORKING_MEMORY.md` is not present.
