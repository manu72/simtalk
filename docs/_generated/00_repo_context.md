# Repository Context

Generated by docsync on 2026-05-24.

## Git Context

- Recent commits focus on Vercel API routing/deploy hardening, typecheck fixes, LiveKit room lifecycle fixes, and PR feedback on room identity/validation:
  - `6e45e5c` fixed a Vercel API hang by switching to `getRequestListener`.
  - `7b55745` fixed Vercel API routing for the non-Next.js Vite project.
  - `735dcb8` and `1573ef3` fixed typecheck and Vercel API handling issues.
  - `f5a1d93`, `38d873b`, `39bc281`, and `994901d` refined LiveKit room behavior and validation.
  - `28b499c` was a minor README update before this pass.
- `git status --short` at the start of this docsync pass showed a pre-existing local modification to `.claude/settings.local.json`. That file is not part of this README sync.
- `WORKING_MEMORY.md` is not present.

## Project State

- Status: pre-MVP private Phase 1 / Phase 1.5 prototype.
- Current implemented slice:
  - pnpm workspace with `frontend`, `backend`, and `shared/types`.
  - Shared Zod contracts for modes, BCP-47 language tags, realtime token requests/responses, room creation/token requests/responses, API errors, health responses, and route constants.
  - Hono backend with `GET /health`, `POST /realtime/token`, `POST /rooms`, and `POST /rooms/:roomId/token`.
  - Server-side OpenAI realtime translation client-secret minting and LiveKit room/JWT minting.
  - Strict configured-origin CORS, baseline security headers, `Cache-Control: no-store` on token-like responses, sanitized OpenAI/LiveKit error mapping, and in-memory per-client rate limiting.
  - React/Vite frontend with Lobby, Listener, Turn-about, Practice, Remote Room, transcript sheet, dev drawer, and Summary surfaces.
  - Browser-native WebRTC setup using microphone capture or a supplied remote media stream, `RTCPeerConnection`, SDP exchange directly with OpenAI, translated audio playback, transcript delta handling, and cleanup.
  - LiveKit remote rooms for two participants: the frontend joins a LiveKit room, publishes the local microphone, and pipes a subscribed remote microphone track into the OpenAI translation session.
  - Browser-local listener/practice recording, transcript copy/download, and audio download when a recording exists.
  - 70+ language catalog with automatic source detection for Listener mode and explicit language pairs for Turn-about, Practice, and room translation.
  - GitHub Actions CI and Vercel deploy workflows committed under `.github/workflows/`.
  - Vitest and Playwright coverage for shared contracts, backend config/routes/services/Vercel adapter, frontend clients/WebRTC/LiveKit modules, React app flows, and mocked browser E2E flows.
- Still pending or limited:
  - Manual realtime validation with real OpenAI and LiveKit credentials, microphone permissions, and supported browsers.
  - Broader browser/device validation for local recording, remote-room audio routing, and audio download behavior.
  - Durable abuse protection beyond in-memory rate limiting.
  - Real in-repo authentication, accounts, persistence, and 3-10 participant rooms.

## Source Files Reviewed

- Product/docs: `README.md`, `PRD.md`, `System_Architecture.md`, `CLAUDE.md`, `docs/ux-redesign-plan.md`, and previous `docs/_generated/*.md`.
- Agentic memory: `.agentic/PROJECT_BRIEF.md`, `.agentic/MEMORY_INDEX.md`, and `.agentic/SUBSYSTEMS/*.md`.
- Root config/tooling: `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `tsconfig.base.json`, `eslint.config.js`, `playwright.config.ts`, `.gitignore`, `.prettierignore`, `components.json`, `vercel.json`, `.github/workflows/*.yml`, and `scripts/agentic/*`.
- Vercel adapter: `api/index.ts` and `api/tsconfig.json`.
- Backend: `backend/package.json`, `backend/.env.example`, `backend/vitest.config.ts`, `backend/src/app.ts`, `backend/src/config.ts`, `backend/src/server.ts`, `backend/src/middleware/*.ts`, `backend/src/routes/*.ts`, `backend/src/services/openAiRealtime.ts`, and `backend/src/services/liveKitRooms.ts`.
- Frontend: `frontend/package.json`, `frontend/.env.example`, `frontend/index.html`, `frontend/vite.config.ts`, `frontend/src/main.tsx`, `frontend/src/App.tsx`, `frontend/src/realtimeTokenClient.ts`, `frontend/src/roomTokenClient.ts`, `frontend/src/realtimeTranslationSession.ts`, `frontend/src/liveKitRemoteRoomSession.ts`, active brand/session/screen components, and `frontend/src/styles/tokens.css`.
- Shared: `shared/types/package.json`, `shared/types/vitest.config.ts`, and `shared/types/src/index.ts`.
- Tests: `tests/shared/unit/index.test.ts`, backend unit/integration tests including routes and Vercel adapter coverage, frontend unit/component/support tests including LiveKit room coverage, and `tests/e2e/home.spec.ts`.

## Config And Environment

- Package manager: pnpm 10.16.1 via root `packageManager`.
- Node engine: Node.js >= 22.
- Workspace packages:
  - `@simtalk/frontend`
  - `@simtalk/backend`
  - `@simtalk/shared-types`
- Active frontend runtime deps are React/React DOM, `livekit-client`, and the workspace shared-types package. Tailwind, shadcn, Radix Slot, `class-variance-authority`, and `tailwind-merge` are not active package dependencies.
- Backend runtime deps include Hono, `@hono/node-server`, `livekit-server-sdk`, and the workspace shared-types package.
- Root dependencies include Hono and `@hono/node-server` because the Vercel `api/index.ts` adapter imports backend source directly and is typechecked by `pnpm typecheck:api`.
- Backend env template includes OpenAI, LiveKit, CORS, TTL, transcription, and rate-limit settings. It does not include `SESSION_SECRET`, `APP_URL`, or `VERCEL_PROTECTION_BYPASS_SECRET`.
- Frontend env template contains `VITE_API_BASE_URL`; production single-project Vercel deployments should set it to `/api`.
- In-repo CI/deploy exists: `.github/workflows/ci.yml`, `.github/workflows/vercel-deploy.yml`, and `vercel.json`.
- No Dockerfile, Cloud Run config, database config, ORM, migration system, queue, worker, or object storage config was found.
