# Architecture And Tech Stack Map

Generated by docsync on 2026-05-23.

## Architecture Summary

SimTalk is a TypeScript pnpm workspace for a private, pre-MVP realtime speech-to-speech translation prototype. Phase 1 has three runtime boundaries:

1. Browser app (`frontend/`)
   - Renders the lobby, live session, transcript, developer drawer, and summary surfaces.
   - Requests a short-lived OpenAI realtime translation credential from the backend.
   - Captures microphone audio after user action.
   - Opens a direct WebRTC session with OpenAI using the client secret.
   - Plays translated audio, renders transcript deltas, records local browser audio where implemented, and tears down media resources.

2. Hono backend (`backend/`)
   - Exposes `GET /health` and `POST /realtime/token`.
   - Validates requests with shared Zod schemas.
   - Reads `OPENAI_API_KEY` server-side only.
   - Calls OpenAI's realtime translation client-secret endpoint.
   - Applies CORS, security headers, sanitized error mapping, `Cache-Control: no-store`, and in-memory rate limiting.
   - Does not proxy, store, or inspect audio/transcript content.

3. OpenAI Realtime Translate
   - Uses `gpt-realtime-translate` through `/v1/realtime/translations`.
   - Accepts browser SDP offers at the translations calls endpoint.
   - Handles speech recognition, translation, synthesized translated audio, and transcript deltas.
   - Public docs describe dynamic voice adaptation for general tone, pitch, and style. They do not promise exact voice cloning or guaranteed speaker-identity/gender matching.

## Runtime Flow

```text
Browser UI
  -> POST /realtime/token
  -> Hono validates request and applies rate limit
  -> Hono calls OpenAI client-secret endpoint with OPENAI_API_KEY
  -> Hono returns short-lived client secret and translation calls URL
  -> Browser captures microphone and sends SDP offer directly to OpenAI
  -> OpenAI returns SDP answer, translated audio, and transcript deltas
  -> Browser renders/transcribes/records locally and cleans up on stop
```

## Tech Stack

| Layer | Current technology |
| --- | --- |
| Language | TypeScript strict |
| Package manager | pnpm 10.16.1 |
| Runtime | Node.js 22+, evergreen browsers with WebRTC |
| Frontend | React 19.2.6, Vite 7.3.3 |
| Frontend styling | `frontend/src/styles/tokens.css`, CSS variables, brand primitives, inline component styles |
| Backend | Hono 4.12.21, `@hono/node-server` 2.0.3 |
| Shared contracts | Zod, `@simtalk/shared-types` |
| Realtime translation | OpenAI `gpt-realtime-translate` |
| Test tooling | Vitest, React Testing Library, jsdom, Playwright |
| Deployment target | Vercel in Phase 1, out-of-repo settings |

Tailwind CSS and shadcn/ui are not installed in the active frontend package. `components.json` and `frontend/src/components/ui/*` are scaffold remnants unless revived later.

## Entrypoints

- `frontend/src/main.tsx` renders the React app and imports `frontend/src/styles/tokens.css`.
- `frontend/src/App.tsx` is the frontend session orchestrator.
- `backend/src/server.ts` starts the Hono server with `@hono/node-server`.
- `backend/src/app.ts` wires middleware, routes, and shared error handling.
- `shared/types/src/index.ts` is the cross-boundary contract hub.

## Storage, Messaging, And Jobs

- No database exists in Phase 1.
- No server-side persistence of audio, transcripts, or PII exists.
- No queue, worker, cache, object storage, or migration system exists.
- Browser-local transcript/audio downloads are implemented for current session artifacts only.

## Tests And CI

- Tests live under root `tests/`, not colocated with packages.
- Per-package Vitest configs point at root test folders.
- Playwright starts the frontend only and mocks token/OpenAI WebRTC network boundaries.
- No `.github/workflows/`, Vercel config, Dockerfile, or Cloud Run config exists in-repo.

## Uncertainties

- Manual live OpenAI validation is still pending.
- Deployment implementation is still out-of-repo.
- The backend validates `mode` and `sourceLanguage`, but currently forwards only `targetLanguage` and transcription model settings to OpenAI for token minting.
